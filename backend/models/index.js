// models/index.js
// -------------------------------------------------
// Models contain raw SQL queries — no business logic.
// They interact directly with the database and return
// results to the controllers.
//
// This file is a placeholder index explaining the
// models/ folder purpose. Individual model files
// (e.g. eventModel.js, bookingModel.js) will be
// added as we build each feature.
// -------------------------------------------------

// Example structure of a model file:
//
// const db = require('../config/db');
//
// const getAllEvents = async () => {
//   const [rows] = await db.query('SELECT * FROM event_types');
//   return rows;
// };
//
// module.exports = { getAllEvents };

module.exports = {};
